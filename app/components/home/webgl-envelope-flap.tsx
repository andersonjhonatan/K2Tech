"use client";
import {CSSProperties,MutableRefObject,useEffect,useRef} from "react";
type Props={progress:MutableRefObject<number>};
const canvasStyle:CSSProperties={position:"absolute",zIndex:6,left:0,top:0,width:"100%",height:"62%",display:"block",pointerEvents:"none",filter:"drop-shadow(0 7px 9px rgba(0,0,0,.48))"};
export function WebGLEnvelopeFlap({progress}:Props){const canvas=useRef<HTMLCanvasElement>(null);useEffect(()=>{const c=canvas.current;if(!c)return;const gl=c.getContext("webgl",{alpha:true,antialias:true,premultipliedAlpha:true});if(!gl)return;const vs=`attribute vec2 a;uniform float p;varying float light;varying float backMix;varying float curlShade;void main(){float row=(1.0-a.y)*.5;float halfW=1.0-row;float x=a.x*halfW;float travel=smoothstep(0.0,1.0,p);float fold=1.0-travel;float d=max(0.0,row-fold);float active=smoothstep(0.0,.085,d);float radius=.17+.035*sin(travel*3.14159);float ang=min(3.14,d*(6.8+4.2*travel));float yy=a.y;float z=0.0;if(active>0.0){float base=1.0-2.0*fold;yy=base-2.0*radius*sin(ang);z=radius*(1.0-cos(ang))*2.75;}float hingeLock=1.0-smoothstep(0.0,.085,row);z*=1.0-hingeLock;float persp=1.0/(1.0+max(z,0.0)*.20);gl_Position=vec4(x*persp,yy*persp,z*.12,1.0);float crest=pow(max(0.0,sin(ang)),3.0);light=.55+.34*cos(ang)+crest*.16;backMix=smoothstep(1.8,2.9,ang);curlShade=crest*active;}`;const fs=`precision mediump float;varying float light;varying float backMix;varying float curlShade;void main(){vec3 dark=vec3(.030,.057,.102);vec3 lit=vec3(.135,.195,.305);vec3 back=vec3(.19,.23,.31);vec3 front=mix(dark,lit,clamp(light,0.0,1.0));vec3 col=mix(front,back,backMix*.30);col*=1.0-curlShade*.22;gl_FragColor=vec4(col,1.0);}`;const sh=(t:number,s:string)=>{const x=gl.createShader(t)!;gl.shaderSource(x,s);gl.compileShader(x);return x};const pr=gl.createProgram()!;gl.attachShader(pr,sh(gl.VERTEX_SHADER,vs));gl.attachShader(pr,sh(gl.FRAGMENT_SHADER,fs));gl.linkProgram(pr);gl.useProgram(pr);const rows=72,v:number[]=[],ind:number[]=[];for(let y=0;y<=rows;y++){const yy=1-y/rows*2;v.push(-1,yy,1,yy)}for(let y=0;y<rows;y++){const i=y*2;ind.push(i,i+1,i+2,i+1,i+3,i+2)}const vb=gl.createBuffer();gl.bindBuffer(gl.ARRAY_BUFFER,vb);gl.bufferData(gl.ARRAY_BUFFER,new Float32Array(v),gl.STATIC_DRAW);const ib=gl.createBuffer();gl.bindBuffer(gl.ELEMENT_ARRAY_BUFFER,ib);gl.bufferData(gl.ELEMENT_ARRAY_BUFFER,new Uint16Array(ind),gl.STATIC_DRAW);const al=gl.getAttribLocation(pr,"a");gl.enableVertexAttribArray(al);gl.vertexAttribPointer(al,2,gl.FLOAT,false,0,0);const pl=gl.getUniformLocation(pr,"p");let raf=0;const draw=()=>{const d=Math.min(devicePixelRatio,1.5),w=Math.max(1,Math.round(c.clientWidth*d)),h=Math.max(1,Math.round(c.clientHeight*d));if(c.width!==w||c.height!==h){c.width=w;c.height=h}gl.viewport(0,0,w,h);gl.clearColor(0,0,0,0);gl.clear(gl.COLOR_BUFFER_BIT|gl.DEPTH_BUFFER_BIT);gl.enable(gl.DEPTH_TEST);gl.depthFunc(gl.LEQUAL);gl.uniform1f(pl,progress.current);gl.drawElements(gl.TRIANGLES,ind.length,gl.UNSIGNED_SHORT,0);raf=requestAnimationFrame(draw)};draw();return()=>cancelAnimationFrame(raf)},[progress]);return <canvas ref={canvas} style={canvasStyle} aria-hidden="true"/>}
